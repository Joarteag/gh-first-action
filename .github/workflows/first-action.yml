name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello WOrld!"
      - name: Print goodbay
        run: echo "Done - bye!"
